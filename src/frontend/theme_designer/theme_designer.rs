#[derive(Debug, Clone)]
pub enum ThemeMsg {
    LoadCurrent,
    SaveWallpaper,
    ToggleWallpaper(bool),
    Variant(bool),
    BackendMenu,
    Option(&'static str, &'static str),
    DesignClose,
    RoleSelect(u8),
    RoleFilter(String),
    Hue(f32),
    SV(f32, f32),
    DragEnd,
    HexInput(String),
    HexSubmit,
    Recent(String),
    Preset(String),
    SeedGen,
    NameInput(String),
    Reset,
    ResetColour,
    SaveTheme,
    SaveApply,
    LoadSaved(String),
    DeleteSaved(String),
}
